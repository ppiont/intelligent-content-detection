# OpenAI API Key for LLM reasoning in hybrid mode
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# LLM Model (default: gpt-4o-mini for speed/cost/vision, can use gpt-4o for quality)
# Note: gpt-5-nano doesn't support vision (images), so use gpt-4o-mini or gpt-4o
LLM_MODEL=gpt-4o-mini

# Roboflow API Key for YOLOv11 model inference
# Get your API key from: https://app.roboflow.com/settings/api
ROBOFLOW_API_KEY=your_roboflow_api_key_here

# Roboflow Model Configuration
# Model ID in format: workspace/project/version
ROBOFLOW_MODEL_ID=roof-dmg-a1b1a/3

# Roboflow confidence threshold (0-100)
ROBOFLOW_CONFIDENCE=40

# Use hybrid vision mode (YOLO + LLM reasoning)
# Set to "false" to use pure OpenAI vision instead
USE_HYBRID=true
